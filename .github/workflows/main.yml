name: CICD
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build:
    runs-on: Ubuntu-20.04
    env:
      APP_ENV: test
      REGION: eu-west-1
    outputs:
      app_env: ${{ env.APP_ENV }}
      aws_region: ${{ env.REGION }}

    steps:
      - name: Cloning repository
        uses: actions/checkout@v2
        
      - name: test
        id: build-image
        run: echo "test ..."

      # - name: ssh-key
      #   run: |
      #     mkdir ~/.ssh
      #     echo "${SSH_KEY}" > ~/.ssh/id_rsa && chmod 600 ~/.ssh/id_rsa
      #     ssh-keyscan github.com > ~/.ssh/known_hosts
      #   env:
      #     SSH_KEY: ${{ secrets.SSH_KEY }}
      
      # - name: test keys
      #   run: |
      #     cat ~/.ssh/id_rsa
      #     git clone git@github.com:opositatest/aula-api.git

  
  deploy:
    needs: build
    runs-on: Ubuntu-20.04
    env:
      REGION2: ${{ needs.build.outputs.aws_region }}

    steps:
      - run: echo  "$REGION2 $APP_ENV2"
        env:
          APP_ENV2: ${{ needs.build.outputs.app_env }}

